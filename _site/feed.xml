<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2025-10-02T10:42:58+08:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">ALRCMt个人网站</title><subtitle>ALRCMt build Jekyll site with the GitBook style now.
</subtitle><author><name>ALRCMt</name></author><entry><title type="html">注意事项</title><link href="http://localhost:4000/jekyll/2025-06-11-LoveFormMe.html" rel="alternate" type="text/html" title="注意事项" /><published>2025-06-11T00:00:00+08:00</published><updated>2025-06-11T00:00:00+08:00</updated><id>http://localhost:4000/jekyll/LoveFormMe</id><content type="html" xml:base="http://localhost:4000/jekyll/2025-06-11-LoveFormMe.html"><![CDATA[<p>以下为我实际搭建过程中的一些“小问题”（并不）和小巧思
<br /></p>

<h2 id="01pve-安装时卡死">01.PVE 安装时卡死</h2>

<p>如果你有一张独立显卡，那么在安装 PVE 时可能会卡在 Loading Driver…，这是因为缺少显卡驱动导致的</p>

<p>解决方法：</p>

<ul>
  <li>启动 Proxmox VE 安装程序
启动计算机并进入 Proxmox VE 的引导程序菜单</li>
  <li>选择安装选项：
在引导菜单中，使用箭头键选择“Install Proxmox VE (Terminal UI)”选项</li>
  <li>编辑引导参数：
按下键盘上的 e 键进入编辑模式</li>
  <li>修改 Linux 引导行：
使用箭头键导航到以 linux 开头的那一行。
将光标移动到该行的末尾</li>
  <li>添加 nomodeset 参数：
在该行的末尾，确保与最后一个参数之间有一个空格，然后输入 nomodeset。
启动安装程序：
完成编辑后，按下 Ctrl + X 或 F10 键（具体取决于系统提示）以启动安装程序</li>
</ul>

<p>将通过禁用图形化模块解决该问题</p>

<h2 id="02pve-网卡莫名其妙掉线问题-不确定">02.<em>PVE 网卡莫名其妙掉线问题 不确定</em></h2>

<p><del>网上看到的原因基本是 intel 的网卡所致，怀疑是驱动兼容性问题</del>
有可能，不确定</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 先安装工具</span>
apt <span class="nt">-y</span> <span class="nb">install </span>ethtool
ethtool <span class="nt">-K</span> eno1 tso off gso off
<span class="c"># 修改vi /etc/network/interfaces ，在iface eno1 inet manual下新增post-up ethtool -K eno1 tso off gso off</span>
auto lo
iface lo inet loopback

iface eno1 inet manual
        post-up ethtool <span class="nt">-K</span> eno1 tso off gso off   <span class="c">#  新增这句</span>

auto vmbr0
iface vmbr0 inet static
        address 192.168.1.33/24
        gateway 192.168.0.1
        bridge-ports eno1
        bridge-stp off
        bridge-fd 0

<span class="nb">source</span> /etc/network/interfaces.d/<span class="k">*</span>
</code></pre></div></div>

<h2 id="03ssh-功能开启问题">03.ssh 功能开启问题</h2>

<p>因为 ubuntu 虚拟机的控制台与本机粘贴板不互通，又不想安装其它插件，于是打算用 windows 的 cmd 远程 ssh，但是 ubuntu 的 ssh 功能死活打不开，最终发现他妈的命令中是<code class="language-plaintext highlighter-rouge">ssh</code>而不是<code class="language-plaintext highlighter-rouge">sshd</code></p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 首先启动SSH服务</span>
systemctl start ssh
<span class="c"># 确认SSH服务已启动，输入以下命令查看SSH服务状态</span>
systemctl status ssh
</code></pre></div></div>

<h2 id="04pve8-概要面板显示-cpu-温度">04.PVE8 概要面板显示 CPU 温度</h2>

<p>通过 shell 脚本自动配置，省时省力省心
运行这段指令：</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">(</span>curl <span class="nt">-Lf</span> <span class="nt">-o</span> /tmp/temp.sh https://raw.githubusercontent.com/a904055262/PVE-manager-status/main/showtempcpufreq.sh <span class="o">||</span> curl <span class="nt">-Lf</span> <span class="nt">-o</span> /tmp/temp.sh https://mirror.ghproxy.com/https://raw.githubusercontent.com/a904055262/PVE-manager-status/main/showtempcpufreq.sh<span class="o">)</span> <span class="o">&amp;&amp;</span> <span class="nb">chmod</span> +x /tmp/temp.sh <span class="o">&amp;&amp;</span> /tmp/temp.sh remod
</code></pre></div></div>

<p>然后刷新页面就可以了</p>

<p>安装 CPU 温度检测软件 sensors</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>apt <span class="nb">install </span>lm-sensors <span class="nt">-y</span>
</code></pre></div></div>

<p>传感器探测</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sensors-detect
</code></pre></div></div>

<p>全部选择 yes 即可，可能其中一个地方提示 ENTER ，按 回车键 即可</p>

<p>ISA adapter：CPU 温度信息</p>

<p>acpitz-acpi-0：主板温度信息</p>

<p>nvme-pci-0200：nvme 固态硬盘温度（如果有安装的话）普通的 sata 固态硬盘不会显示</p>

<p>最气人的是不知道为什么我这里死活不显示 CPU 各核心温度，所以其他我也懒得配置了，平时使用</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sensors
</code></pre></div></div>

<p>看一下基本温度就行了</p>

<blockquote>
  <p>后续：已破案，原来是 AMD 的 U 不会显示各核心温度<br />
如果你是 amd 的 CPU，当输入<code class="language-plaintext highlighter-rouge">sensors</code>后如下图<br />
SYSTIN 是主板南桥温度<br />
AUXTIN 是电源温度（前提是你有传感器，否则数据无效）<br />
CPUTIN 是主板监控的 CPU 温度<br />
Tctl/Tdie 是 CPU 为降温虚标的高温，目的是使风扇转速加快<br />
详细见 https://ngabbs.com/read.php?tid=42423467&amp;rand=200</p>
</blockquote>

<p><img src="./photo/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202025-08-09%20224901.png" alt="" width="700px" /></p>

<h2 id="05ubuntu-空间仅占用一半">05.Ubuntu 空间仅占用一半</h2>

<p>在安装 ubuntu server 的过程中，默认只占用一半磁盘空间，如果想补救如下</p>

<p>使用 <code class="language-plaintext highlighter-rouge">df -h</code> 命令显示文件系统的总空间和可用空间信息</p>

<p>使用 <code class="language-plaintext highlighter-rouge">sudo vgdisplay</code> 命令查看发现 Free PE / Size 还有 剩余空间</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="nb">sudo </span>lvextend <span class="nt">-l</span> +100%FREE /dev/mapper/ubuntu--vg-ubuntu--lv <span class="c"># 调整逻辑卷的大小</span>

 <span class="nb">sudo </span>resize2fs /dev/mapper/ubuntu--vg-ubuntu--lv <span class="c"># 调整文件系统的大小</span>

 <span class="nb">df</span> <span class="nt">-h</span> <span class="c"># 再次查看，确认文件系统的总空间大小调整成功</span>
</code></pre></div></div>

<h2 id="06pve-更换-apt-源后报错">06.PVE 更换 apt 源后报错</h2>

<p>在修改<code class="language-plaintext highlighter-rouge">/etc/apt/sources.list.d/debian.sources</code>后<br />
pve 的”更新&gt;存储库”页面报错出现 “\u{200b}” 的字样<br />
是零宽空格导致，在复制粘贴的过程中产生，自己多检查几遍</p>

<p>至于报错 <em>没有启用 proxmox ve 存储库没有得到任何更新</em>
忽视，反正也不更新</p>

<h2 id="07c6-state导致pve崩溃">07.C6-State导致PVE崩溃</h2>

<p>不知道为什么PVE运行一段时间会莫名其妙崩溃，且系统日志没有记录<br />
事后调查发现是<strong>AMD Ryzen 1700X（初代锐龙/Zen 1）启用了C6 State模式自动节能卡死</strong><br />
参考文献：<br />
https://blog.csdn.net/qq_33026779/article/details/145600293<br />
https://forum.proxmox.com/threads/pve-6-raidz2-freeze-every-day-ryzen-7-1700.66629/</p>

<p>解决方法：进入主板bios，将<strong>Global C-State Control</strong>设置为disabled</p>

<h2 id="08bios时区错误">08.BIOS时区错误</h2>

<p>在设置自动开机的时候，我发现主板BIOS时间与PVE系统时间差了8小时<br />
调查原因：<br />
PVE默认将BIOS时间视为UTC时间，而上海时间（CST）是UTC+8，因此系统会自动将BIOS时间减去8小时以“对齐”时区
解决方法：</p>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>timedatectl set-local-rtc 1
<span class="c"># 作用：直接设置硬件时钟（RTC）使用本地时间（上海时间），停止UTC转换</span>
</code></pre></div></div>
<p>验证命令：</p>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>timedatectl | <span class="nb">grep</span> <span class="s2">"RTC in local TZ"</span>
<span class="c"># 若显示yes即生效</span>
</code></pre></div></div>

<h2 id="09pve开机显示zfs导入错误">09.PVE开机显示ZFS导入错误</h2>

<p>在将硬盘直通给TrueNAS后，PVE仍会尝试挂载ZFS，同时访问<strong>可能</strong>会导致<strong>数据损坏</strong><br />
所以应该确保 PVE 宿主机不主动挂载或导入该 ZFS 池，而是由 TrueNAS 虚拟机独占访问</p>

<p>立即导出 ZFS 池(如果已导入)</p>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>zpool <span class="nb">export </span>MtData
</code></pre></div></div>
<p>因为我的PVE系统没有使用ZFS作为根文件系统，而是使用<strong>LVM+ext4</strong><br />
所以我直接简单粗暴，完全移除ZFS工具</p>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt purge zfsutils-linux zfs-zed <span class="nt">-y</span>
</code></pre></div></div>

<h2 id="10pve降低功耗">10.PVE降低功耗</h2>
<p>CPU电源策略调整：</p>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 安装 cpupower</span>
apt <span class="nb">install </span>linux-cpupower

<span class="c"># 查看支持的 CPU 电源模式</span>
<span class="nb">cat</span> /sys/devices/system/cpu/cpu0/cpufreq/scaling_available_governors

<span class="c"># 查看当前的 CPU 电源模式</span>
<span class="nb">cat</span> /sys/devices/system/cpu/cpu0/cpufreq/scaling_governor

<span class="c"># CPU实时频率查看</span>
watch <span class="nt">-n</span> 1 cpupower monitor

<span class="c"># 查看当前所有CPU的信息</span>
cpupower <span class="nt">-c</span> all frequency-info

<span class="c"># 设置所有CPU为节能模式</span>
cpupower <span class="nt">-c</span> all frequency-set <span class="nt">-g</span> powersave

<span class="c"># 设置所有CPU为性能模式</span>
cpupower <span class="nt">-c</span> all frequency-set <span class="nt">-g</span> performance
</code></pre></div></div>

<p>|  电源模式  | 解释说明 |
|  —-  | —-  |
| performance  | 性能模式，将 CPU 频率固定工作在其支持的较高运行频率上，而不动态调节 |
| userspace  | 系统将变频策略的决策权交给了用户态应用程序，较为灵活 |
| powersave  | 省电模式，CPU 会固定工作在其支持的最低运行频率上 |
| ondemand  | 按需快速动态调整 CPU 频率，没有负载的时候就运行在低频，有负载就高频运行 |
| conservative  | 与 ondemand 不同，平滑地调整 CPU 频率，频率的升降是渐变式的，稍微缓和一点 |
| schedutil  | 负载变化回调机制，后面新引入的机制，通过触发 schedutil sugov_update 进行调频动作 |</p>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
<span class="c"># 我这里设置CPU电源策略为模式conservative</span>
cpupower <span class="nt">-c</span> all frequency-set <span class="nt">-g</span> conservative

</code></pre></div></div>
<p>设置机械硬盘休眠：
编辑 /etc/rc.local，在exit 0之前加如下</p>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>hdparm <span class="nt">-B</span> 192 /dev/sda   <span class="c"># 设置APM级别为192</span>
hdparm <span class="nt">-S</span> 240 /dev/sda   <span class="c"># 长时间不活动停转（20分钟）</span>
</code></pre></div></div>]]></content><author><name>ALRCMt</name></author><category term="Jekyll" /><summary type="html"><![CDATA[以下为我实际搭建过程中的一些“小问题”（并不）和小巧思]]></summary></entry><entry><title type="html">应用配置</title><link href="http://localhost:4000/jekyll/2025-06-10-Apply.html" rel="alternate" type="text/html" title="应用配置" /><published>2025-06-10T00:00:00+08:00</published><updated>2025-06-10T00:00:00+08:00</updated><id>http://localhost:4000/jekyll/Apply</id><content type="html" xml:base="http://localhost:4000/jekyll/2025-06-10-Apply.html"><![CDATA[<blockquote>
  <p>小提示：<br />
在 Linux 系统中 Ctrl + Shift + C 复制； Ctrl + Shift + V: 粘贴<br />
在 vi/vim 编辑器中，<code class="language-plaintext highlighter-rouge">:wq</code>是保存并退出，<code class="language-plaintext highlighter-rouge">:q!</code>是不保存退出<br />
在 nano 编辑器中 Ctrl + W 快捷键是查找文本，但是与 web 界面关闭页面冲突，所以可以用 Ctrl + Q 代替，Ctrl + X 是退出，会询问是否保存
sudo 不能提升 cd 的权限</p>
</blockquote>

<h2 id="pve-配置">PVE 配置</h2>

<h3 id="1设置-pve-的-apt-源">1.设置 PVE 的 APT 源</h3>

<p><strong>8.x 版本设置</strong>
PVE 的默认软件源是他的企业服务地址，我们个人使用需要将其换成国内的软件源
编辑<code class="language-plaintext highlighter-rouge">/etc/apt/sources.list</code>，替换如下</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>deb https://mirrors.ustc.edu.cn/debian bookworm main contrib
deb https://mirrors.ustc.edu.cn/debian bookworm-updates main contrib
deb https://mirrors.ustc.edu.cn/debian-security bookworm-security main contrib
</code></pre></div></div>

<p>将 PVE 的企业源 <code class="language-plaintext highlighter-rouge">/etc/apt/sources.list.d/pve-enterprise.sources</code> 注释掉<br />
将 PVE 的 Ceph 源<code class="language-plaintext highlighter-rouge">/etc/apt/sources.list.d/ceph.list</code>，替换如下</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>deb https://mirrors.tuna.tsinghua.edu.cn/proxmox/debian/ceph-quincy bookworm no-subscription
</code></pre></div></div>

<p>在<code class="language-plaintext highlighter-rouge">/etc/apt/sources.list.d/</code>下创建 pve-no-sub.list 文件，填上以下内容</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>deb https://mirrors.tuna.tsinghua.edu.cn/proxmox/debian bookworm pve-no-subscription
</code></pre></div></div>

<p><strong>9.x 版本设置</strong>
在<code class="language-plaintext highlighter-rouge">/etc/apt/sources.list.d/debian.sources </code>中注释掉原有配置，添加以下</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Types: deb
URIs: https://mirrors.tuna.tsinghua.edu.cn/debian
Suites: trixie trixie-updates trixie-backports
Components: main contrib non-free non-free-firmware
Signed-By: /usr/share/keyrings/debian-archive-keyring.gpg

Types: deb-src
URIs: https://mirrors.tuna.tsinghua.edu.cn/debian
Suites: trixie trixie-updates trixie-backports
Components: main contrib non-free non-free-firmware
Signed-By: /usr/share/keyrings/debian-archive-keyring.gpg
</code></pre></div></div>

<blockquote>
  <p>因为 9.x 版本 BUG 太多了，这下面的源换不换随便，我也不清楚</p>
</blockquote>

<blockquote>
  <p>将 PVE 的企业源 <code class="language-plaintext highlighter-rouge">/etc/apt/sources.list.d/pve-enterprise.sources</code> 注释掉<br />
将 PVE 的 Ceph 源 <code class="language-plaintext highlighter-rouge">/etc/apt/sources.list.d/ceph.sources</code> 也替换成清华源</p>
</blockquote>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Types: deb
URIs: https://mirrors.tuna.tsinghua.edu.cn/proxmox/debian/ceph-squid
Suites: trixie
Components: main
Signed-By: /usr/share/keyrings/proxmox-archive-keyring.gpg
</code></pre></div></div>

<blockquote>
  <p>在 <code class="language-plaintext highlighter-rouge">/etc/apt/sources.list.d</code> 目录下创建 pve-no-subscription.sources 文件，填上以下内容</p>
</blockquote>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Types: deb
URIs: https://mirrors.tuna.tsinghua.edu.cn/proxmox/debian/pve
Suites: trixie
Components: pve-no-subscription
Signed-By: /usr/share/keyrings/proxmox-archive-keyring.gpg
</code></pre></div></div>

<blockquote>
  <p>如果修改 apt 源后报错，<a href="#06pve-%E6%9B%B4%E6%8D%A2-apt-%E6%BA%90%E5%90%8E%E6%8A%A5%E9%94%99">解决方法</a></p>
</blockquote>

<h3 id="2网络唤醒-wol">2.网络唤醒 WOL</h3>

<p>需要在 BIOS 中开启 WOL 功能，各主板设置方法不同，自己上网查去<br />
默认情况下，PVE 的网络唤醒是禁用的，需要手动打开才可以网络唤醒</p>

<p>安装 ethtool 工具</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>apt <span class="nb">install </span>ethtool
</code></pre></div></div>

<p>查看网卡信息</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
ethtool <span class="o">[</span>网卡名称] <span class="c"># 观察输出结果Supports Wake-on 与 Wake-on</span>

<span class="c"># supports wake-on 判断该网卡是否支持 WOL 唤醒，若值为 pumbg 则表示支持 WOL</span>
<span class="c"># Wake-on 值为 d 则表示 WOL 禁用状态，g 则为开启，PVE 默认为 d</span>
</code></pre></div></div>

<p>开启 WOL 网络唤醒</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ethtool <span class="nt">-s</span> eth0 wol g
</code></pre></div></div>

<p>由于每次开机时，Wake-on 的值都会重置为 d，因此需要在开机时自动运行开启 WOL 的命令</p>

<p>编辑 /etc/rc.local 文件</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>
ethtool <span class="nt">-s</span> eth0 wol g
​
<span class="nb">exit </span>0
</code></pre></div></div>

<p>赋予运行权限</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">chmod</span> +x /etc/rc.local
</code></pre></div></div>

<p>目前我用的是蒲公英自带的<strong>向日葵远程开机</strong></p>

<blockquote>
  <p>冷知识：蒲公英重启后，服务器第一次无开机法远程唤醒</p>

  <h3 id="3自动开关机">3.自动开关机</h3>
  <p>自动开机：在主板BIOS设置唤醒事件管理，选择时间，我选择每天9点</p>
</blockquote>

<p>自动关机： 
编辑 root 用户的 crontab</p>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>crontab <span class="nt">-e</span>
</code></pre></div></div>
<p>添加自动关机任务</p>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 每天凌晨2:30执行关机</span>
30 2 <span class="k">*</span> <span class="k">*</span> <span class="k">*</span> /sbin/shutdown <span class="nt">-h</span> +5 <span class="s2">"系统将在5分钟后关机进行日常维护，请保存您的工作"</span>
</code></pre></div></div>

<h2 id="旁路由-r300a-配置">旁路由 R300A 配置</h2>

<h3 id="1开启路由器的-wan-口转发">1.开启路由器的 WAN 口转发</h3>

<p>打开贝锐蒲公英后台：https://www.pgybox.com/zh/intelligentNetwork/forwardingSettings<br />
找到转发设置，打开 WAN 口入站路由转发</p>

<h3 id="2配置异地组网">2.配置异地组网</h3>

<p>打开蒲公英管理平台：https://console.sdwan.oray.com/zh/main
创建网络，添加硬件成员 R300A<br />
添加网络成员，然后在需要连接的设备上下载贝锐蒲公英客户端，登录添加的网络成员账号，即可开启组网</p>

<p><img src="./photo/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202025-08-09%20150111.png" alt="" width="700px" /></p>

<p>贝锐蒲公英客户端下载：https://pgy.oray.com/download#visitor</p>

<h2 id="truenas-配置">TrueNAS 配置</h2>

<h3 id="1实现硬盘直通">1.实现硬盘直通</h3>

<p>教程地址：<a href="https://github.com/firemakergk/aquar-build-helper/blob/master/details/pve%E7%A1%AC%E7%9B%98%E7%9B%B4%E9%80%9A.md">pve 硬盘直通</a></p>

<blockquote>
  <p>取消硬盘直通的方法<br />
pve 的 web 界面选择虚拟机的“硬件”，选择指定硬盘，点击“分离”</p>
</blockquote>

<h3 id="2配置存储池及用户设置">2.配置存储池及用户设置</h3>

<p>教程：<br />
<a href="https://www.bilibili.com/video/BV1cK411z7dx/?spm_id_from=333.1007.top_right_bar_window_custom_collection.content.click&amp;vd_source=2a55d6df129012c2f31dfcad634bc9de">【司波图】TrueNAS SCALE 教程，第一章——简单用起来</a></p>

<h3 id="3smb-共享配置">3.SMB 共享配置</h3>

<p>在 TrueNAS 的 Web 页面上进入共享页面<br />
打开 Windows（SMB）共享服务<br />
<em>确认在用户配置创建的用户勾选了 SMB 用户选项</em>
添加 SMB 共享，选择共享目录</p>

<p><img src="./photo/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202025-08-10%20133538.png" alt="" width="700px" /></p>

<p>在同一个局域网中，在文件管理器显示各个硬盘页面的空白处右键，选择“添加一个网络位置”</p>

<p><img src="./photo/屏幕截图 2025-08-10 133618.png" alt="" width="700px" /></p>

<p>一番下一步后会让你输入地址，填写 truenas 的服务地址然后又是一番下一步，最后会询问你用户名和密码<br />
这时候就填写你在 TrueNas 上新创建的用户的名称和密码即可</p>

<p><img src="./photo/屏幕截图 2025-08-10 134332.png" alt="" width="400px" />
<img src="./photo/屏幕截图 2025-08-10 134349.png" alt="" width="300px" /></p>

<ul>
  <li>小提示：地址从你的存储池开始计算，如我这里就是/Mt 而不是 /mnt/MtData/Mt</li>
</ul>

<p>最终结果：</p>

<p><img src="./photo/屏幕截图 2025-08-10 134441.png" alt="" width="300px" /></p>

<h3 id="4nfs-共享配置">4.NFS 共享配置</h3>

<blockquote>
  <p><em>注意！！！</em><br />
<em>这里请配合 Ubuntu 挂载使用</em></p>
</blockquote>

<p>同上，打开 UNIX（NFS）共享服务<br />
添加 NFS 共享，选择共享目录<br />
如果想方便一点，选择配置服务，勾选允许非 root 挂载<br />
之后操作在 Ubuntu 系统完成</p>

<h2 id="ubuntu-配置">Ubuntu 配置</h2>

<h3 id="1安装-docker-最折磨人的一集其实还好">1.安装 Docker <del><em>最折磨人的一集</em></del>(其实还好)</h3>

<p><del>由于国内网络问题（最折磨），Docker 使用阿里云镜像源安装</del><br />
目前 Docker 安装的网络问题得到了改善</p>

<p>在 ubuntu 控制台输入以下命令<br />
<em>粘贴板不互通无法复制？请用 ssh 连接。无法 ssh？<a href="#03ssh-%E5%8A%9F%E8%83%BD%E5%BC%80%E5%90%AF%E9%97%AE%E9%A2%98">解决方案</a></em></p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
<span class="c"># 在安装 Docker 之前，我们需要安装一些必要的依赖包。运行以下命令：</span>
<span class="nb">sudo </span>apt <span class="nb">install </span>apt-transport-https ca-certificates curl software-properties-common

apt <span class="nb">install</span> <span class="nt">-y</span> docker.io  docker-compose <span class="c"># 安装docker</span>
docker version <span class="c"># 验证安装</span>

</code></pre></div></div>

<p>现在 Docker 已经安装完毕，但是拉取镜像的网络环境依旧<del>十分</del>很他妈糟糕，所以先不拉取 hello-world 测试，等会教学配置加速地址</p>

<h3 id="2安装-docker-可视化工具-dpanel">2.安装 Docker 可视化工具 DPanel</h3>

<p>DPanel 是一款<strong>支持中文</strong>的 Docker 可视化插件<br />
使用如下命令下载 Dpanel lite 版镜像<br />
官方教程：https://dpanel.cc/install/docker</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker pull registry.cn-hangzhou.aliyuncs.com/dpanel/dpanel:lite
</code></pre></div></div>

<p>之后使用如下命令运行 Dpanel 容器</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run <span class="nt">-d</span> <span class="nt">--name</span> dpanel <span class="nt">--restart</span><span class="o">=</span>always <span class="se">\</span>
 <span class="nt">-p</span> 80:80 <span class="nt">-p</span> 443:443 <span class="nt">-p</span> 8807:8080 <span class="nt">-e</span> <span class="nv">APP_NAME</span><span class="o">=</span>dpanel <span class="se">\</span>
 <span class="nt">-v</span> /var/run/docker.sock:/var/run/docker.sock <span class="se">\</span>
 <span class="nt">-v</span> /home/dpanel:/dpanel registry.cn-hangzhou.aliyuncs.com/dpanel/dpanel:lite
</code></pre></div></div>

<p>DPanel 管理地址：Ubuntu 网络地址加端口 8807
快速使用教程：<a href="https://www.bilibili.com/video/BV1gDc9eaEBv/?spm_id_from=333.337.search-card.all.click&amp;vd_source=2a55d6df129012c2f31dfcad634bc9de">一款更适合国人的 Docker 可视化管理工具</a></p>

<h3 id="3docker-镜像仓库加速">3.Docker 镜像仓库加速</h3>

<p><strong>命令行加速</strong></p>

<p>编辑<code class="language-plaintext highlighter-rouge">/etc/docker/daemon.json</code>，加入以下内容</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">{</span>
  <span class="s2">"registry-mirrors"</span>: <span class="o">[</span>
    <span class="s2">"https://docker.1ms.run"</span>,
    <span class="s2">"https://docker.m.daocloud.io"</span>,
    <span class="s2">"https://registry.cn-hangzhou.aliyuncs.com"</span>
  <span class="o">]</span>,
<span class="o">}</span>
</code></pre></div></div>

<p>保存并退出</p>

<p><strong>面板加速</strong></p>

<p>在 DPanel 内选择<strong>仓库管理</strong>，编辑 Docker Hub 仓库<br />
添加加速地址，下面有推荐加速地址<br />
<img src="./photo/屏幕截图 2025-08-10 151557.png" alt="" width="700px" /></p>

<p>如果你要加速别的仓库，请<strong>添加仓库</strong>，然后配置加速，<a href="https://dpanel.cc/manual/image-registry">可添加仓库</a></p>

<h3 id="4数据卷的创建挂载查看删除">4.数据卷的创建、挂载、查看、删除</h3>

<p>挂载数据卷可能比较好操作<br />
如果你偏爱用命令行操作，那么如下</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker volume create my-vol <span class="c"># 创建一个数据卷</span>

docker volume <span class="nb">ls</span> <span class="c"># 查看所有的数据卷</span>

docker volume inspect my-vol <span class="c"># 查看指定数据卷的信息</span>

docker volume <span class="nb">rm </span>my-vol <span class="c"># 删除数据卷</span>
</code></pre></div></div>

<p>将数据卷挂载在容器的固定目录</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run <span class="nt">-it</span> <span class="nt">-v</span> <span class="o">[</span>数据卷名字]:[容器目录] <span class="o">[</span>镜像名称]
</code></pre></div></div>

<p>如果你想用图形化操作，如下
打开 DPanel 的 web 页面，选择储存管理</p>

<p><img src="./photo/屏幕截图 2025-08-10 145725.png" alt="" width="700px" /></p>

<p>然后创建储存卷，名称随便，其它默认，然后确定</p>

<p><img src="./photo/屏幕截图 2025-08-10 145806.png" alt="" width="300px" /></p>

<h3 id="5将-truenas-存储池挂载到指定目录">5.将 TrueNAS 存储池挂载到指定目录</h3>

<blockquote>
  <p><em>注意！！！ 配合<a href="#4nfs-%E5%85%B1%E4%BA%AB%E9%85%8D%E7%BD%AE">NFS 共享配置</a>、<a href="#4%E6%95%B0%E6%8D%AE%E5%8D%B7%E7%9A%84%E5%88%9B%E5%BB%BA%E6%8C%82%E8%BD%BD%E6%9F%A5%E7%9C%8B%E5%88%A0%E9%99%A4">数据卷的创建挂载</a>使用，将存储池挂到数据卷的挂载点</em></p>
</blockquote>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt update <span class="c"># 更新系统存储库索引</span>
<span class="nb">sudo </span>apt <span class="nb">install </span>nfs-common <span class="c"># 安装 NFS 客户端包</span>

<span class="nb">sudo </span>mount <span class="o">[</span>NFS _IP]:/[NFS_export] <span class="o">[</span>Local_mountpoint] <span class="c"># 将NFS服务器共享目录挂载到客户端的挂载点目录</span>

<span class="c"># NFS_IP 是 NFS 服务器的 IP 地址</span>
<span class="c"># NFS_export 是 NFS 服务器上的共享目录</span>
<span class="c"># Local_mountpoint 是客户端系统上的挂载点目录</span>

mount | <span class="nb">grep </span>nfs <span class="c"># 查看已挂载的NFS共享目录，确认挂载成功</span>

<span class="nb">sudo </span>nano /etc/fstab <span class="c"># 设置 NFS 文件在系统启动时自动挂载</span>

<span class="c"># 然后使用以下格式在 /etc/fstab 文件中添加条目</span>

<span class="o">[</span>NFS _IP]:[NFS_export] <span class="o">[</span>Local_mountpoint] nfs defaults 0 0   <span class="c"># NFS 服务器:服务器共享目录 目录挂载点 nfs 默认 0 0</span>

<span class="c"># 然后保存并退出</span>
</code></pre></div></div>

<p>卸载 NFS 挂载</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>umount <span class="o">[</span>mount_point] <span class="c"># [mount_point]是挂载目录</span>

<span class="c"># 如果该目录正在被使用或者已经被其他进程打开，你可能会收到一个错误消息。在这种情况下，可以尝试使用以下命令强制取消挂载</span>

umount <span class="nt">-f</span> <span class="o">[</span>mount_point]

mount | <span class="nb">grep </span>nfs <span class="c"># 最后，检查挂载是否成功取消</span>
</code></pre></div></div>

<h3 id="6docker-部署-resilio-sync">6.Docker 部署 Resilio Sync</h3>

<p>通过 DPanel 图形化操作，打开容器列表，创建容器，然后拉取镜像，镜像地址<code class="language-plaintext highlighter-rouge">resilio/sync:latest</code></p>

<p><img src="./photo/屏幕截图 2025-08-10 151320.png" alt="" width="600px" />
<img src="./photo/屏幕截图 2025-08-10 152541.png" alt="" width="600px" />
等待镜像拉取完成，大部分设置已经设置好了
只需要绑定端口，如图</p>

<p><img src="./photo/屏幕截图 2025-08-10 152732.png" alt="" width="500px" /></p>

<p>然后挂载数据卷，选择添加映射目录<br />
左侧填你创建的数据卷名称，右侧填容器内目录，目录最好是/mnt/sync/folders/*，不然可能会没权限</p>

<p><img src="./photo/屏幕截图 2025-08-10 152741.png" alt="" width="500px" /></p>

<p>在运行配置页面，重启策略选择<strong>未手动停止则重启</strong><br />
最后选择<strong>提交</strong>，容器就创建并运行了</p>

<p>Resilio Sync 管理地址：Ubuntu 网络地址加端口 8888<br />
使用教程（更多还是自己摸索吧）：https://zhuanlan.zhihu.com/p/745919095</p>

<h3 id="7docker-部署-immich">7.Docker 部署 immich</h3>
<p>通过 Dpanel 图形化操作，使用Docker Compose部署</p>

<p><img src="./photo/屏幕截图 2025-08-16 145611.png" alt="" width="650px" /></p>

<p>选择Compose，<strong>创建任务</strong>，名称随便<br />
下载yaml文件和env文件导入：https://immich.app/docs/install/docker-compose</p>

<blockquote>
  <p>因为我要用immich管理相机备份，文件在link.nas数据卷，所以编辑yaml<br />
修改如下内容，将宿主机路径 /var/lib/docker/volumes/link.nas/_data 挂载到容器内的 /mnt/nas<br />
``` shell
services:
  immich-server:
    volumes:
      - ./immich-data:/usr/src/app/upload  # 保留原始卷
      - /var/lib/docker/volumes/link.nas/_data:/mnt/nas  # 新增挂载点
    # 其他配置保持不变…</p>
</blockquote>

<p>immich-microservices:
    volumes:
      - ./immich-data:/usr/src/app/upload  # 保留原始卷
      - /var/lib/docker/volumes/link.nas/_data:/mnt/nas  # 新增挂载点
    # 其他配置保持不变…</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
填写环境变量   
`UPLOAD_LOCATION`：图片存储位置  
`DB_DATA_LOCATION`：数据库文件存储位置  
`# TZ`：时区

这是我填的  
&lt;img src="./photo/屏幕截图 2025-08-16 150025.png" alt="" width="380px"/&gt;  
&lt;img src="./photo/屏幕截图 2025-08-16 152101.png" alt="" width="200px"/&gt;  
然后**提交**，点击详情，选择**启动**  
&lt;img src="./photo/屏幕截图 2025-08-16 153548.png" alt="" width="350px"/&gt;

等待镜像拉取完成，如果出现`运行中(4)`就好了  
&lt;img src="https://raw.githubusercontent.com/ALRCMt/MtAIO-Build/cb99109050678a8dc9f7933bb70bc5681e4f1084/photo/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202025-08-16%20154355.png" alt="" width="700px"/&gt;  
管理地址：Ubuntu 网络地址加端口 2283  
管理我的相机备份：添加外部图库，选择路径是之前映射到容器内的路径  
然后扫描，等待，完成，其他教程自己网上搜去  
官方文档：https://immich.app/docs/overview/welcome   

&gt; 一定要使四个容器都拉取完成，可能有点慢  
&gt; 如果出现报错`error response from daemon: get "https://ghcr.io/v2/": not found`  
&gt; 那就是网络问题，如果你已经配置了加速  
&gt; 那么，似乎没有更好的办法，**祈祷**吧  
&lt;img src="https://raw.githubusercontent.com/ALRCMt/MtAIO-Build/cb99109050678a8dc9f7933bb70bc5681e4f1084/photo/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202025-08-16%20161707.png" alt="" width="200px"/&gt;

&gt; 小贴士：上半夜Docker Hub的网络很差

### 8.Docker 部署 V2rayA

**请确保你有可用稳定的代理地址**

选择**Compose**，创建任务，yaml如下

``` shell
version: '3.8' 
services:
  v2raya:
    image: mzz2017/v2raya
    restart: always
    container_name: v2raya
    network_mode: host 
    privileged: true 
    cap_add:    
      - NET_ADMIN
      - SYS_MODULE
    ports:
      - "2017:2017"
      - "7890:7890"
    volumes:
      - ./etc:/etc/v2raya
</code></pre></div></div>
<p><strong>提交</strong>，<strong>启动</strong>，等待镜像拉取完成<br />
浏览器访问地址http://ip:2017<br />
打开v2rayA，创建管理员账号<br />
以创建或导入的方式导入节点，导入支持节点链接、订阅链接</p>

<p>点击右上角的设置，开启ip转发和端开分享，根据自己的需求设置<br />
<img src="https://raw.githubusercontent.com/ALRCMt/MtAIO-Build/cb99109050678a8dc9f7933bb70bc5681e4f1084/photo/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202025-08-17%20232009.png" alt="" width="450px" /></p>

<p>然后回到首页，选择一个节点点击连接后，在点击左上角的启动按钮启动即可<br />
<img src="https://raw.githubusercontent.com/ALRCMt/MtAIO-Build/cb99109050678a8dc9f7933bb70bc5681e4f1084/photo/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202025-08-17%20231749.png" alt="" width="750px" /></p>

<h3 id="9配置代理">9.配置代理</h3>

<p>安装proxychains工具</p>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt update 
<span class="nb">sudo </span>apt <span class="nb">install </span>proxychains
</code></pre></div></div>
<p>编辑 <code class="language-plaintext highlighter-rouge">/etc/proxychains.conf</code><br />
在 /etc/proxychains.conf 增加socks5，同时注释掉socks4的内容：</p>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># socks4 127.0.0.1 9050</span>
socks5 <span class="o">[</span>部署v2raya的服务器ip] 端口号
</code></pre></div></div>
<p>例如我在 IP 为 10.168.1.165 的服务器上，部署了v2rayA，同时开启了端口分享<br />
并且查看到了socks5的端口号是20170，那么就将proxychains的配置文件改为：</p>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># socks4 127.0.0.1 9050</span>
socks5 10.168.1.165 20170 <span class="c"># 如果是本机使用，ip可改为127.0.0.1</span>
</code></pre></div></div>
<p>访问谷歌测试是否成功</p>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>proxychains curl https://www.google.com
</code></pre></div></div>
<p>出现以下内容就算成功<br />
<img src="https://raw.githubusercontent.com/ALRCMt/MtAIO-Build/cb99109050678a8dc9f7933bb70bc5681e4f1084/photo/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202025-08-17%20234722.png" alt="" width="550px" />
<br /></p>

<h3 id="10docker部署qbittorrent-webui">10.Docker部署qBittorrent WebUI</h3>
<p>通过Docker Compose部署</p>

<p>编辑yaml文件</p>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>version: <span class="s2">"3"</span>
services:
  qbittorrent:
    image: linuxserver/qbittorrent
    container_name: qbittorrent
    environment:
      - <span class="nv">PUID</span><span class="o">=</span>0
      - <span class="nv">PGID</span><span class="o">=</span>0
      - <span class="nv">TZ</span><span class="o">=</span>Asia/Shanghai 
      - <span class="nv">UMASK_SET</span><span class="o">=</span>022
      - <span class="nv">WEBUI_PORT</span><span class="o">=</span>8085  <span class="c"># Web UI端口</span>
      - <span class="nv">TORRENTING_PORT</span><span class="o">=</span>26881 <span class="c"># 监听端口，默认6881，修改为20000-65535区间值，下同</span>
    volumes:
      - /vol1/1000/docker/qbittorrent/config:/config  <span class="c"># 冒号左侧修改参照上文</span>
      - /vol2/1000/Download2:/downloads2
      - /vol3/1000/Download3:/downloads3          <span class="c">#同上</span>
    ports:
      - 8085:8085  <span class="c"># 同上面Web UI端口一致</span>
      - 26881:26881
      - 26881:26881/udp
    restart: unless-stopped
</code></pre></div></div>
<p><strong>提交</strong>，<strong>启动</strong>
管理界面：http://ip:8085
用户admin，临时密码可以在日志中看见</p>
<blockquote>
  <p>小提示<br />
如果遇到管理界面打不开或显示<strong>unauthorized</strong>
请想办法换一个内核的浏览器（自己想办法）<br />
登录后在设置&gt;WebUI中去掉 <em>启用跨站请求伪造 (CSRF) 保护</em> 兵保存</p>
</blockquote>

<p>怎么用自己搜去</p>

<h3 id="11docker部署openlist">11.Docker部署OpenList</h3>
<p>OpenList是Alist的社区版本，这里选择OpenList</p>

<p>创建容器，拉取<code class="language-plaintext highlighter-rouge">openlistteam/openlist:latest</code>，绑定端口，把要用的文件夹挂好<br />
然后http://ip:5244 ，登录<br />
官方文档：https://openlistteam.github.io/docs/zh/
使用教程网上一大把</p>]]></content><author><name>ALRCMt</name></author><category term="Jekyll" /><summary type="html"><![CDATA[小提示： 在 Linux 系统中 Ctrl + Shift + C 复制； Ctrl + Shift + V: 粘贴 在 vi/vim 编辑器中，:wq是保存并退出，:q!是不保存退出 在 nano 编辑器中 Ctrl + W 快捷键是查找文本，但是与 web 界面关闭页面冲突，所以可以用 Ctrl + Q 代替，Ctrl + X 是退出，会询问是否保存 sudo 不能提升 cd 的权限]]></summary></entry><entry><title type="html">系统配置</title><link href="http://localhost:4000/jekyll/2025-06-09-System.html" rel="alternate" type="text/html" title="系统配置" /><published>2025-06-09T00:00:00+08:00</published><updated>2025-06-09T00:00:00+08:00</updated><id>http://localhost:4000/jekyll/System</id><content type="html" xml:base="http://localhost:4000/jekyll/2025-06-09-System.html"><![CDATA[<p><strong>参考<a href="https://github.com/firemakergk/aquar-build-helper?tab=readme-ov-file#%E7%B3%BB%E7%BB%9F%E5%AE%89%E8%A3%85">@生火人 firemaker</a></strong>
<strong>主要是以下系统的安装与配置</strong></p>

<ul>
  <li><a href="#%E5%AE%89%E8%A3%85-proxmox-virtual-environment"><strong>Proxmox Virtual Environment</strong></a></li>
  <li><a href="#%E5%AE%89%E8%A3%85-truenas-scale"><strong>TrueNAS scale</strong></a></li>
  <li><a href="#%E5%AE%89%E8%A3%85-ubuntu-server"><strong>Ubuntu Server</strong></a>
    <blockquote>
      <p>特殊安装：<a href="#%E5%AE%89%E8%A3%85-windows10-%E4%B8%8D%E9%9C%80%E8%A6%81"><strong>Windows10</strong></a><br />
因为我的笔记本坏了一段时间，又没有别的电脑，刚好有一块多的 m.2 硬盘<br />
于是我额外安装了 win10 系统在这块硬盘上，它与 PVE 是独立的</p>
    </blockquote>
  </li>
</ul>

<p><br /></p>

<h2 id="安装-proxmox-virtual-environment">安装 Proxmox Virtual Environment</h2>

<p><strong>1.下载镜像</strong></p>

<p>pve 的镜像官网下载页面：https://www.proxmox.com/en/downloads/category/iso-images-pve</p>

<p><del>直接下载最新版本即可</del>推荐 8.x 版本，9.x 版本 BUG 太 ™ 多了</p>

<p><strong>2.制作启动盘</strong></p>

<p>推荐使用 Etcher 制作启动盘，你要用 Rufus 也随便</p>

<p>Etcher 下载地址：<a href="https://pve.proxmox.com/pve-docs/pve-admin-guide.html#installation_prepare_media">https://pve.proxmox.com/pve-docs/pve-admin-guide.html#installation_prepare_media</a></p>

<p><strong>3.安装 PVE</strong></p>

<p>将启动盘插入物理机，重启进入 BIOS（进 bios 哪个键自己网上搜对应主板去），选择从启动盘启动，然后进入安装流程</p>

<p>安装过程中 pve 会让你设置一个域名，并不关键，按默认即可</p>

<p>安装流程的官方文档：https://pve.proxmox.com/pve-docs/pve-admin-guide.html#installation_installer</p>

<p>安装过程中卡死？解决方法：<a href="#%E6%B3%A8%E6%84%8F%E4%BA%8B%E9%A1%B9">PVE 安装时卡死</a></p>

<p><strong>4.验证安装</strong></p>

<p>PVE 安装完成后，首先在你的物理机屏幕上会显示出服务的 IP 地址（大概类似<a href="https://youripaddress:8006/">https://192.168.X.XXX:8006/</a>)，注意是 https 协议，在局域网下打开这个地址，你就可以看到 PVE 的 WEB 控制台了</p>

<p><img src="./photo/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202025-08-09%20110507.png" alt="" /></p>

<p>默认用户是 root，密码是你安装时设置的，语言设置为中文</p>

<p><br /></p>

<h2 id="安装-truenas-scale">安装 TrueNAS scale</h2>

<p>TrueNAS scale 相较于可以直接搭载 Docker 服务，虽然使用 PVE 这种虚拟化平台作为底层系统，但是 TrueNAS scale 能提供更多选择（其实就是我根本没看是 core 还是 scale</p>

<p><strong>1.下载镜像</strong></p>

<p>TrueNAS SCALE 的下载页面： https://www.truenas.com/download-truenas-community-edition</p>

<p>刚进入时会提示你注册，点击右下角的 No Thanks 即可看到下载链接了。推荐直接下载最新版本即可。</p>

<p><strong>2.上传镜像到 PVE</strong></p>

<p>在左侧的树状图中选择 pve 节点的 local 存储，在右侧选择 ISO 镜像，然后点上传，上传你的在上一步下载的 TrueNAS scale ISO 文件。你可以提前下载后面两节需要用到的镜像，然后集中上传，这可以节省很多时间。</p>

<p><strong>3.创建虚拟机</strong></p>

<p>在 pve 的 web 页面的右上角点击创建虚拟机，为 TrueNAS 创建一个虚拟机。</p>

<p>通用信息配置中勾选右下角的 Advanced，并把这个虚拟机设置为开机自启动，然后设置启动顺序为 1，等待时间 60(秒)，需要注意的是这里的等待时间指的是这台虚拟机开机后等待下一台虚拟机开机的时间，而不是他与上一台虚拟机开机的等待时间。<strong>设置合理的启动顺序和等待时间非常重要</strong>，否则会影响上层服务的存储池挂载</p>

<p><img src="./photo/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202025-08-09%20030601.png" alt="" width="700px" /></p>

<p>操作系统配置页面选择你上传的 TrueNAS IOS 镜像，并设置操作系统类型为 Other</p>

<p><img src="./photo/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202025-08-09%20030634.png" alt="" width="700px" /></p>

<p>系统配置页面我的配置如下：</p>

<p><img src="./photo/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202025-08-09%20030653.png" alt="" width="700px" /></p>

<p>系统磁盘空间我分配了 32G，其他配置项没有需要修改的地方</p>

<p><img src="./photo/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202025-08-09%20030701.png" alt="" width="700px" /></p>

<p>CPU 分配了 2 核，另外 CPU 类型选择了 host，在单机情况下这样设置可以获得最小性能损耗</p>

<blockquote>
  <p><em>在 8.x 版本的系统中，如果使用的是混合架构的 CPU 如 12 代 i7，可以直接在界面的 CPU Affinity 设置中指定绑定的 CPU 序号</em><br />
<em>查看 CPU 多核类别的方法是使用<code class="language-plaintext highlighter-rouge">lscpu -e</code>命令，可以看到 E 核的 MAXMHZ 会低于 P 核</em><br />
<em>（这里我并没有这么配置，所以我不太清楚具体配置）</em></p>
</blockquote>

<p><img src="./photo/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202025-08-09%20030809.png" alt="" width="700px" /></p>

<p>内存方面由于 TrueNAS 推荐使用 16G 以上内存空间，但是我总共只有 16G 内存，所以分配了 8G，可以正常使用</p>

<p><img src="./photo/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202025-08-09%20030759.png" alt="" width="700px" /></p>

<p>网络方面我暂时修改默认配置，以后应该可以将网络类型换成 VirtlIO 以提升性能</p>

<p>进入到确认页面后点击创建就可以了</p>

<p><img src="./photo/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202025-08-09%20030836.png" alt="" width="700px" /></p>

<p>虚拟机创建成功后，打开他的 console 应该就可以看到安装提示了。</p>

<p><strong>4.安装 TrueNAS SCALE</strong></p>

<p>推荐教程 https://post.smzdm.com/p/a6d8m6vg/</p>

<p>官方文档：https://www.truenas.com/docs/scale/25.04/gettingstarted/install/</p>

<p>由于在一些 USB 设备连接不稳定的情况下，TrueNAS 虚拟机会收到 USB 热插拔的影响而死机，所以安装完成以后打开虚拟机的 Options（选项）页，双击 Hotplug（热拔插）设置项，把 USB 选项的勾选去掉。</p>

<p><strong>5.验证安装</strong></p>

<p>TrueNAS 安装成功后在局域网中使用浏览器打开提示中的地址应该就可以看到 TrueNAS 的 Web 页面了</p>

<p><img src="./photo/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202025-08-09%20030930.png" alt="" /></p>

<p>默认用户名是 truenas_admin，密码是在安装时设置</p>

<p><br /></p>

<h2 id="安装-ubuntu-server">安装 Ubuntu Server</h2>

<p><strong>1.下载镜像</strong></p>

<p>由于我们使用 ubuntu 的作用主要是承载各种服务而非直接与之交互，所以选择没有 GUI 的 Ubuntu Server 版本。</p>

<p>Ubuntu Server 下载页面：https://cn.ubuntu.com/download/server/step1</p>

<p>选择最新的 LTS 版本即可</p>

<p><strong>2.上传镜像</strong></p>

<p>与 TrueNAS 章节的上传操作一致，不再重复</p>

<p><strong>3.创建虚拟机</strong></p>

<p>与 TrueNAS 章节的创建操作类似，内存我分配了 4GB</p>

<p><strong>4.安装 Ubuntu Server</strong></p>

<p>推荐教程：https://blog.csdn.net/FungLeo/article/details/148370828</p>

<p>Ubuntu Server 官方文档的安装指引：https://ubuntu.com/server/docs/install/step-by-step</p>

<p>安装时有几点需要注意：</p>

<ol>
  <li>Mirror 设置时，Ubuntu 现在默认为国内源地址，如果不是的话请更换成你所在的地区最稳定的地址</li>
  <li>ubuntu 安装默认只占用一半空间，需自己勾选上 <em><a href="#05ubuntu-%E7%A9%BA%E9%97%B4%E4%BB%85%E5%8D%A0%E7%94%A8%E4%B8%80%E5%8D%8A">已经安装完成？补救方法</a></em></li>
  <li>SSH 设置时勾选 Install SSH Server</li>
  <li>Snaps 页面不要选择任何软件进行安装</li>
  <li>在 Ubuntu 安装开始执行一段时间后（大概几分钟），会开始拉取软件源信息，没必要等待，直接选择”跳过并重启”即可</li>
</ol>

<p><strong>5.验证安装</strong></p>

<p>在 PVE 中找到 Ubuntu 的虚拟机，并进入 Console 界面，多按动几次回车键，如果看到类似的提示，则输入你安装时设置的用户名和密码。如果登录成功则说明系统正常运行了</p>

<p><img src="./photo/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202025-08-09%20122037.png" alt="" /></p>

<h2 id="安装-windows10-不需要">安装 Windows10 <em>不需要</em></h2>
<p><strong>1.下载镜像</strong></p>

<p>Win10 iso 镜像下载地址：https://www.microsoft.com/zh-cn/software-download/windows10</p>

<p><strong>2.制作启动盘</strong></p>

<p>我这里使用微 PE 来做启动盘</p>

<p>微 PE 下载地址：https://www.wepe.com.cn/download.html</p>

<p>下载后启动应用写入 U 盘，再将已下载的 iso 镜像复制到 U 盘</p>

<p><strong>3.安装 windows10</strong></p>

<p>将启动盘插入物理机，开机进入 BIOS，选择从启动盘启动</p>

<p>进 PE 后官方安装教程：https://www.wepe.com.cn/ubook/installtool.html</p>

<p>安装后拔出硬盘，我这里不多赘述了</p>

<p><strong>4.验证安装</strong></p>

<p>安装完成后，开机 bios 调整引导硬盘顺序即可选择启动 win10 或 PVE</p>]]></content><author><name>ALRCMt</name></author><category term="Jekyll" /><summary type="html"><![CDATA[参考@生火人 firemaker 主要是以下系统的安装与配置]]></summary></entry><entry><title type="html">其他条件</title><link href="http://localhost:4000/jekyll/2025-06-08-Others.html" rel="alternate" type="text/html" title="其他条件" /><published>2025-06-08T00:00:00+08:00</published><updated>2025-06-08T00:00:00+08:00</updated><id>http://localhost:4000/jekyll/Others</id><content type="html" xml:base="http://localhost:4000/jekyll/2025-06-08-Others.html"><![CDATA[<h3 id="局域网">局域网</h3>

<p>保持网络稳定</p>

<p>首先是内网 IP 地址的稳定，通常情况下路由器会根据设备的 MAC 地址给局域网中的网络设备分配一个稳定的内网 IP，但并不尽然，一旦局域网的网关将服务器中的设备 IP 刷新了，那么会导致你无法访问自己的服务，也无法正常访问存储池</p>

<p>如果你的本地网络出现了这种情况，则需要修改路由器的设置来使局域网 IP 稳定下来</p>

<h3 id="公网-ip">公网 IP</h3>

<p>取得一个公网 IP 对于提升系统易用性的好处是很大的。最直接的好处就是你可以通过 DDNS 工具来让自己在公网上直接访问到服务，否则你就需要借助各种内网穿透手段达到相同的目的</p>

<p>如果你无法获取公网 IPv4 地址也不用担心，随着 IPv6 的发展，现在的主流家用宽带都已经能够分配 IPv6 地址，而 IPv6 地址天生就是公网地址。你可以打电话给宽带的运营人员，把家里的光猫设置为桥接模式，并在自己的路由器中打开 ipv6 功能。通过重启路由器或者等待一段时间（几小时或几天），等到整个局域网中的设备都使用了桥接模式下的新 IPv6 地址。此时就可以去专门 IPv6 测试网站上测试一下你的 IPv6 连通性如何了，如果测试通过了，你大概率就可以顺利使用 parsec、ddns 等依赖公网 ip 的工具了</p>

<h3 id="场地">场地</h3>

<p>系统运行时不可避免的产生噪音，主要是风扇声以及机械硬盘写入的声音。即使你使用无风扇的低功耗平台来搭建系统，机械硬盘的声音也是不容小觑的，<del>所以强烈不建议将机器放在卧室等休息的地方</del>我就放在卧室</p>]]></content><author><name>ALRCMt</name></author><category term="Jekyll" /><summary type="html"><![CDATA[局域网]]></summary></entry><entry><title type="html">硬件选择</title><link href="http://localhost:4000/jekyll/2025-06-07-HandWare.html" rel="alternate" type="text/html" title="硬件选择" /><published>2025-06-07T00:00:00+08:00</published><updated>2025-06-07T00:00:00+08:00</updated><id>http://localhost:4000/jekyll/HandWare</id><content type="html" xml:base="http://localhost:4000/jekyll/2025-06-07-HandWare.html"><![CDATA[<p><strong>这里全部都是我的硬件配置，可以提供参考</strong></p>

<h3 id="cpu">CPU</h3>

<p>如果只是做个人 NAS，推荐 E5 等低功耗的服务器级 U
我使用的是一颗 AMD R7 1700X，8 核心 16 线程，TDP 为 95W<br />
如果你需要 windows 虚拟机，那么至少需要这台机器在原生运行 Windows 时可以保持流畅，另外建议使用带有核显的 CPU，否则在直通显卡以后你的宿主机控制台会被 windows 占用</p>

<h3 id="内存">内存</h3>

<p>我使用的是金百达银爵 8G*2 DDR4 3200 频率
由于 TrueNas 的推荐配置为 16G 内存（实测 8G 并没有问题），所以建议的最低内存为 16G</p>

<h3 id="独显-可选">独显 可选</h3>

<p>如果你不需要跑 windows，那么可以不配置独显，如上文所述，系统搭建前期建议尽量保留核显给宿主机作为排查问题的窗口，所以如果你有另外的显示需求或者硬件解码需求，还是推荐配备一个独显<br />
独显型号需要按照你的使用方式自行斟酌，例如我选择了 GT710 2G，原因在于可以<del>提升 CPU 性能（能作为 win10 亮机卡）</del></p>

<h3 id="硬盘">硬盘</h3>

<p>系统盘：建议使用固态硬盘以保证系统流畅,我现在先使用铭瑄 120G sata 固态凑合</p>

<p>数据盘：由于文件系统需要长期运行，并会经常伴随读写，所以需要避开叠瓦盘（SMR）。另外由于至少需要组成 RAID1 阵列来保证数据安全，所以至少需要两块数据盘<br />
推荐 NAS 专用盘、监控盘或者企业盘</p>

<h3 id="主板">主板</h3>

<p>搭建个人服务器的话，一般推荐 sata 口多的 itx 主板，服务器级主板最好<br />
我这里用微星 m-atx 主板 B350 mortar 凑合，只有 4 个sata口，后续可以考虑sata扩展卡或HBA卡</p>

<h3 id="电源">电源</h3>

<p>这个随便，只要装得下，不会炸就行<br />
因为机箱比较小而且我用塔式散热器，所以我选择 1U 全模组电源</p>

<h3 id="散热">散热</h3>

<p>能压得住 CPU 温度就行，我使用利民 AX90 SE 塔式散热器，再接几个机箱风扇</p>

<h3 id="机箱">机箱</h3>

<p>这就八仙过海各显神通了，<del>不要也行</del>，我使用星之海的 NAS 专用机箱射手座</p>

<h3 id="旁路由-可选">旁路由 可选</h3>

<p>我使用了一台闲置的蒲公英 R300A 来配置服务器的网络</p>

<p>它的功能：</p>

<ul>
  <li>为服务器提供网络</li>
  <li>利用蒲公英进行异地组网</li>
  <li>ssh 跳板机</li>
</ul>

<p>你可以选择其他设备比如树莓派，或者智能路由器充当这个角色，但强烈建议保持这个网络连接中枢的独立性</p>

<h3 id="ups-可选">UPS 可选</h3>

<p>建议使用 UPS 保证机器的安全。ZFS 在突发断电时有可能出现元数据损坏，这会导致整个阵列既无法使用也<strong>无法恢复</strong><br />
你说得对，所以有好心人送我一个吗</p>

<h3 id="u-盘">U 盘</h3>

<p>你需要一个 U 盘来烧录 pve 的磁盘镜像，作为物理机安装系统的启动盘</p>]]></content><author><name>ALRCMt</name></author><category term="Jekyll" /><summary type="html"><![CDATA[这里全部都是我的硬件配置，可以提供参考]]></summary></entry><entry><title type="html">MtAIO系统介绍及指南</title><link href="http://localhost:4000/jekyll/2025-06-06-About.html" rel="alternate" type="text/html" title="MtAIO系统介绍及指南" /><published>2025-06-06T00:00:00+08:00</published><updated>2025-06-06T00:00:00+08:00</updated><id>http://localhost:4000/jekyll/About</id><content type="html" xml:base="http://localhost:4000/jekyll/2025-06-06-About.html"><![CDATA[<p>本指南作为我自己摸索学习的记录，借鉴了https://github.com/firemakergk/aquar-build-helper 的内容</p>

<p>MtAIO 系统是一套整合了多种开源组件的系统集合，本质上是跑在 Proxmox VE 虚拟化平台上的若干个虚拟机以及其中应用的个人服务器，其源于两年前欲求文件存储与共享的突发奇想，归功于<del>强大的行动力</del>（大嘘）
，在使用了各家网盘、各家云服务器均不尽人意后，搭建了这套系统</p>

<p>作为一个完全的小白，这套系统的搭建帮助我了解学习计算机知识及 linux 系统，配置网络环境，以及成为一位合格的垃圾佬，可以说，我完全是一下一下摸索着搭建这套系统的，因此我认为如果你有兴趣搭建这么一套系统，不论有没有技术基础，也一定可以成功，所以在这里，我会把 MtAIO 系统搭建过程中的大小问题及解决方法记录下来，希望可以帮到你</p>

<p>感谢这些对我有帮助的人：</p>

<p><a href="https://space.bilibili.com/304756911?spm_id_from=333.1387.favlist.content.click">@生火人 firemaker</a></p>

<p><a href="https://space.bilibili.com/204773750/?spm_id_from=333.788.upinfo.head.click">@Intro_iu</a></p>

<p><a href="https://space.bilibili.com/34786453?spm_id_from=333.1387.favlist.content.click">@康文昌</a></p>

<p><a href="https://space.bilibili.com/316183842?spm_id_from=333.1387.favlist.content.click">@技术爬爬虾</a></p>

<p><a href="https://space.bilibili.com/5626102?spm_id_from=333.1387.favlist.content.click">@科技宅小明</a></p>

<p>如果你有疑问，或者发现了我的错误，请联系我</p>

<p><a href="https://qm.qq.com/q/4uVkK9nRPW?personal_qrcode_source=3"><img src="https://img.shields.io/badge/QQ-ALRCMt-white.svg" alt="QQ" /></a>
<a href="mailto:b122330417@163.com"><img src="https://img.shields.io/badge/邮箱-b122330417@163.com-blue.svg" alt="邮箱" /></a></p>

<h2 id="核心功能">核心功能</h2>

<ul>
  <li>Proxmox VE 虚拟化多系统</li>
  <li>预装 TrueNas 并配置多种共享服务（NFS、SMB、WebDAV）</li>
  <li>预装多种 Docker 服务（DPanel<em>docker 可视化、Syncthing</em>文件备份…）</li>
  <li>蒲公英异地组网多平台随时使用</li>
  <li>一套独立的 windows10 系统冗余</li>
  <li><del>装逼</del></li>
</ul>

<h2 id="需求与使用场景-引用自生火人-firemaker">需求与使用场景 <a href="https://github.com/firemakergk/aquar-build-helper?tab=readme-ov-file#%E9%9C%80%E6%B1%82%E4%B8%8E%E4%BD%BF%E7%94%A8%E5%9C%BA%E6%99%AF">引用自@生火人 firemaker</a></h2>

<p>从零纯手工搭建系统的过程很长，因为这是长期沉淀下来的，有很多细节蕴含在其中。然而我更想强调的是，当你走完所有这些路途以后，你和这套系统磨合才真正开始。<strong>一套软件系统是生长在使用者的需求上的，合理且充分的需求就像土壤，是软件系统存活乃至成长的必要条件。</strong></p>

<p>所以搭建系统前你需要问自己一些问题：</p>

<ul>
  <li>你希望使用这套系统解决什么问题？</li>
  <li>这些问题是否有其他更便利的解决方式？</li>
  <li>这套系统的特性是否适合你长期使用？</li>
</ul>

<p>如果这些问题的答案都支持你搭建这套系统，那么请阅读接下来的内容，祝你好运</p>]]></content><author><name>ALRCMt</name></author><category term="Jekyll" /><summary type="html"><![CDATA[本指南作为我自己摸索学习的记录，借鉴了https://github.com/firemakergk/aquar-build-helper 的内容]]></summary></entry></feed>