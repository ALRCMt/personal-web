# frozen_string_literal: true

source "https://rubygems.org"

# gem "rails"

# Windows 和 JRuby 平台不包含 zoneinfo 文件，因此需要捆绑 tzinfo-data gem
platform :mingw, :x64_mingw, :mswin, :jruby do
  gem "tzinfo", "~> 1.2"  # 确保这行存在
  gem "tzinfo-data"
end
